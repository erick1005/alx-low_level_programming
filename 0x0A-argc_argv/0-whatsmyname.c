#include <stdio.h>

/**
 * main - entry
 * @argc: argument count
 * @argv: argument array
 * Return: 0 for success
 */
int main(int __attribute__ ((unused)) argc, char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}

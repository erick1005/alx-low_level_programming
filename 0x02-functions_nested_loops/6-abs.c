#include "main.h"

/**
 * _abs - computes the absolute value of an integer.
 *@r: value to be checked
 * Return:abs_value for success
 */
int _abs(int r)
{

	if (r < 0)
	{
	return (1);
	}
	else
	{
	return (0);
	}

}

#include "main.h"

/**
 * int _islower - checks the character
 *
 * Return:1 for success or 0 for failure
 */
int _islower(int c)
{
	if (c >= 97 && c <= 122)
	{
	return (1);
	}
	else
	{
	return (0);
	}
}

#include "main.h"
#include <string.h>

/**
 * _strcmp - compares strings
 * @s1: first string
 * @s2: second string
 * Return: 0 is strings are equal
 */
int _strcmp(char *s1, char *s2)
{
	return (strcmp(s1, s2));
}
